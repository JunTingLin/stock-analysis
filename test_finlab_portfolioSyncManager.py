# from finlab.portfolio import PortfolioSyncManager

# pm = PortfolioSyncManager()
# pm = PortfolioSyncManager.from_local(name='peterwu')

# pm.update(report, total_balance=180000, odd_lot=True, force_override_difference=False, smooth_transition=True)

# pm.to_local(name='peterwu')

# pm.sync(acc, view_only=True)
